# GENERATED by dabs/generate_resources.py from config/tables.json — DO NOT EDIT BY HAND.
# Re-run the generator after editing config/tables.json (single source of truth, ADR 0004).
variables:
  migration_targets:
    type: complex
    description: Per-table migration metadata (target schema + index columns) generated
      from config/tables.json and consumed by the migration job. Index columns have
      no native postgres_synced_tables field, so they are carried here.
    default:
    - name: members
      app_schema: cicd_proj
      app_role: members_app_ro
      index_columns:
      - member_id
      - plan_code
    - name: providers
      app_schema: cicd_proj
      app_role: providers_app_ro
      index_columns:
      - provider_id
      - specialty_code
