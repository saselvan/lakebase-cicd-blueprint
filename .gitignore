# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# Liquibase secrets
liquibase/liquibase.properties
*.env

# Local planning doc (grill-with-docs) — not shared
CONTEXT.md

# Local planning (specs/tickets) — not shared
.scratch/

# Python (dev venv + caches)
.venv*
__pycache__/
*.pyc
.pytest_cache/

# Liquibase offline-validate byproduct (tracks changesets in a CSV when no DB is used)
liquibase/databasechangelog.csv
