# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# Liquibase secrets
liquibase/liquibase.properties
*.env

# Local planning doc (grill-with-docs) — not shared
CONTEXT.md
